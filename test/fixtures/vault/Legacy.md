# Legacy

A note written before the `@task` marker, seeded so the migration command has
something real to convert. Nothing else in the harness reads it.

- [ ] First task
  - [ ] a step, must stay plain
- [x] Second task
- [ ] @task Already converted
  - [ ] its step

```sh
- [ ] fake, inside a fence
```
